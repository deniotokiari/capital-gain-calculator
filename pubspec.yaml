name: capital_gain_calculator
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  firebase_core: ^2.4.1
  flutter:
    sdk: flutter
  
  sign_up:
    path: ./packages/feature/sign_up
  alphavantage:
    path: ./packages/data/alphavantage
  auth:
    path: ./packages/data/auth
  utility: 
    path: ./packages/core/utility
  cloud_store:
    path: ./packages/data/cloud_store

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
